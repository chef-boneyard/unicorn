# Upstart Unicorn Conf

start on (filesystem and net-device-up IFACE=lo)
stop on runlevel [!2345]

respawn

script
    cd <%= working_directory %>
    bundle exec unicorn -c <%= @name %> -E <%= @rack_env %>
    flock -x 0 < <%= @pid %>.lock
end script
